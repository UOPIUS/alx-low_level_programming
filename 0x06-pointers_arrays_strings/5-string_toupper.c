#include "main.h"

/**
* string_toupper - Converts letters to uppercase
* @s: characters
* Return: string
*/
char *string_toupper(char *s)
{
	int i = 0;

	while (x[i])
	{
		if (x[i] >= '97' && x[i] <= 122)
			x[i] = x[i] - 32;
		i++;
	}
	return (x);
}
