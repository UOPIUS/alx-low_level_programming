#include "main.h"

/**
 * is_palindrome - returns a palindrome
 * @s: characters
 * Return: int
 */

int is_palindrome(char *s)
{
	return check_palindrome(s, 1);
}

/**
 * check_palindrome - checking palindrome pattern
 * @s: string
 * @i: iterator
 * Return: int
 */
int check_palindrome(char *s, int i)
{

}
