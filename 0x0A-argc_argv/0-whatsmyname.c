#include <stdio.h>

/**
 * main - print a program's own name
 * @argc: argument counter
 * @argv: list of argument
 * Result: integer
 */

int main(argc, char *argv[])
{
	printf("%s\n",*argv);
	return (0);
}
